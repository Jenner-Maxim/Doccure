var person = document.querySelector(".chat .all-contact .person");
person.scrollTop = person.scrollHeight - person.clientHeight;

var chat = document.querySelector(".chat-message .chat");
chat.scrollTop = chat.scrollHeight - chat.clientHeight;